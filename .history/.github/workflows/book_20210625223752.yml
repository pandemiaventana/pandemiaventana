name: actualiza_libro

# Only run this when the master branch changes
on:
  push:
    branches:
    - main
  # Para actualizar a las 11, 12, 13, 14, 16 y 20 hrs. con 30 min. de Chile (tiempos en los que suele actualizar el repo del Min. Ciencia)
  schedule:
    - cron: '30 15,16,17,18,20,24 * * *'

# This job installs dependencies, build the book, and pushes it to `gh-pages`
jobs:
  actualiza_libro:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    # Para set de español en locale (ubuntu)
    - name: setLocale
      run: |
        sudo apt-get update
        sudo apt-get install language-pack-es
    
    # Install dependencies
    - name: Set up Python 3.7
      uses: actions/setup-python@v1
      with:
        python-version: 3.7

    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    # Build the book
    - name: Build the book
      run: |
        jupyter-book build .
    # Push the book's HTML to github-pages
    - name: GitHub Pages action
      uses: peaceiris/actions-gh-pages@v3.5.9
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./_build/html
